// ============ LEVEL 10-3 — THE ORGAN LOFT (District 10 · The Aurora Palace · where the cold sits crowned) ============
// THE CLIMB LEVEL (constitution 6a: every district ships one) at the SUMMIT of the beyond-D5 band — the
// ultimate exam, still MAIN-GAME FAIR: hearts-always, every telegraph >=0.6s (pipe inhale 0.7 · herald horn
// 0.7 · knight bow 0.7 · icicle shimmer 0.7 · wisp wake bites 0.8 then FADES · carol verses ARE the clock),
// <=4 simultaneous threat systems at the single worst seam, every clock fixed from level start,
// one-good-run-away, never Kaizo. Up the GREAT FROST ORGAN itself: the ranks launch you loft to loft (the
// chord progression IS the ascent — ride pipe blasts up the registers); between ranks, tapestry climbs and
// a bell-rope; AuroraWisps swim the loft air; a herald plays counterpoint across one landing. The palace
// kept the Instrument in practice for ten thousand years. Nobody ever came to the recital.
// 14 threats + the trio: 2 FrostHerald + 2 MirrorFrost + 2 AuroraWisp + 2 FrostKnight + 1 CarolBoo DUET
// (verses 0/2 — even here, the incomplete round; verse 1's silence is the crossing window) + 4 SpikeIcicle
// (reuse type #2, w6 kit) + 3 FlurryTriplet (mid-boss). EXACTLY CP0 (noLight) + ONE lit lantern at ~52%.
// NO Leap of Faith (both are placed and sacred) · NO warp (the guest chair sits in its own level) · GP idx1.
//
//   BEAT 1 THE NAVE (x -8..28, y0)        — CP0 (noLight). The great console + BENCH (the quiet prop), the
//          FrostChest in its clear pocket, the teaching sign. Nothing bites on the nave floor, ever.
//   BEAT 2 THE TEACHING RANK (x 28..39)   — kit w10Organ, 4 pipes, INTRODUCE solo: board via the console
//          stair, feel the inhale (0.7s glow+draw), ride the blast (vy13 ≈ +3.5) up to Loft 1 (y4.6).
//   BEAT 3 LOFT 1 (x 39..70, y4.6)        — TWIST: the loft air is alive. MirrorFrost #1's hall (feint it,
//          run past), AuroraWisp #1's freezing wake over the walk, the CAROL DUET singing verses 0 and 2
//          of a three-part round (the missing verse is the safe window), two tap-gaps, then the TAPESTRY
//          CLIMB (velvet runners, climb volume) up to Loft 2. EXPERT: bounce a swooping caroler to the lip.
//   BEAT 4 LOFT 2 (x 68..119.5, y9.4)     — THE lantern (x82, ~52%). Then the ESCALATE: an icicle over the
//          loft gap, Herald #1's counterpoint beam across the landing, the icicle slalom, and THE SILENT
//          PIPE (x108) — the one pipe that never plays. GP idx1 waits inside (see the secret note below).
//   BEAT 5 THE GREAT RANK (x 111..120)    — MASTER pt.1: the loft-anchored high rank. Ride the chord up
//          the registers through AuroraWisp #2's ribbon to the choir lip (y14.2) — the FAST line. The
//          BELL-ROPE (w5Chain, x119.6) beside it is the patient line; the rank's candy halo is visible
//          from the rope (the junction itch).
//   BEAT 6 THE CHOIR FLOOR (x 120..163)   — arrival greeted by FrostKnight #1's courtly bow and Herald
//          #2's phrase, then the MID-BOSS: THE FLURRY TRIPLETS — three snowflake-orbs rolling three fixed
//          lanes in staggered thirds (the Gourd Triplets tradition, now crystalline). Stomp each once
//          while the others roll — or dance through and let the court keep its composure. A hanging
//          choir-lamp perch (double-jump, candy-traced) is the refuge. Knight #2 stands the last watch.
//   BEAT 7 THE RECESSIONAL (x 163.8..176) — over the east plinth and down: cornice ledges step down the
//          case's flank (3.4-3.6 drops, free), the long bell-rope for the careful (and the climb BACK for
//          anything left behind), the farewell sign, the gate. The Instrument holds your chord.
//
// ROUTES (2-3 visible, junctions sighted): SAFE = stair->blast->tapestry->bell-rope, every verb taught
// (plus the mercy runner at x38.6: nave -> Loft 1's west lip, so a fall never re-runs the whole rank) ·
// FAST = ride the blasts on first-cycle timing, cross Loft 1 on the missing verse, take the GREAT RANK
// chord ride (candy halo overhead sells it from the rope line) · EXPERT = stomp-bounce Carol #2's swoop
// straight to the Loft 2 lip (bounce vy13 from the dip ≈ y9.8 > lip 9.4 — never required, never hinted
// beyond two high candies). SPEEDRUN NORTH STAR: every mechanic has a fast-line answer — blasts beat
// stairs, the missing verse beats waiting, the chord beats the rope, dancing beats fighting the trio.
// FALL PRICE: no void pits — every fall lands on a floor (the nave runs UNDER the whole climb to the great
// case plinth at x120; recessional falls land on the porch ground at 163.8+). The climb's cost is the
// re-climb, never a cheap heart (w7l3 trawler law). killY never reachable — the cavity under the choir is
// SEALED at both ends. WALK-TO-WIN SEALED (the w8l3 split-floor law): the nave DEAD-ENDS at the west
// plinth (solid y0..13.7 under the choir's lip, flush with its underside) and the porch is reachable ONLY
// from the choir floor above — the gate cannot be reached without climbing the Instrument.
//
// COMPARABLE HEIGHTS (tap 1.8 / held 2.6 / double 3.3 / blast vy13 ≈ +3.5 from the mouth): console stair
// 0.9/0.9 + 0.4 onto pipe0 (2.2) · pipe-to-pipe rises <=1.6 or blast-carried (over-clearance +1.9) · Loft 1
// lip 4.6 from pipe1 top 3.8 blast (apex 7.3, +2.7 clear) or pipe3 2.2 blast (5.7, +1.1) · loft gaps
// 3.0/3.4/3.2 (tap law <=4) · tapestry climb 4.6->10.6 over the 9.4 lip · silent-pipe flange steps 1.5/1.5,
// mouth hop 1.1 · GREAT RANK stair 1.1 (then 0.4 down, 1.1 up); blasts from tops 11.6..13.2 reach apex
// 15.1..16.7 over the 14.2 choir lip (+0.9..+2.5 clear) · bell-rope 10.0->15.2 · choir-lamp perch 16.7
// (double-jump route, 2.5 rise <=3.0, candy-traced) · recessional drops 3.4/3.6/3.6/3.6 (down only).
// HEARTS ALWAYS: beam/wake/swoop/lunge/icicle/roller all cost exactly 1 heart + i-frames; organ blasts and
// pipe mouths are HARMLESS lift (only enemies bite); falls cost re-climb, not hearts. DETERMINISM: both
// rank clocks, both herald periods, the carol round, all four icicle clocks, and all three triplet lanes
// are fixed-phase from level start; the trio + duet run cull:false so the shared clocks NEVER de-sync
// (CarolBoo fleet-audit law); NO Math.random on the critical path (rand() only inside baked deco).
//
// ---- REACH-MATH LEDGER (pinned; re-run the height audit if any number moves) ----
// CP0 (x2, y0, z1.6, body 1.6..2.4): FrostChest x15 ambush = opt-in (kit 1s spawnGrace; flurry lanes
//   2.4..27.6 run at z0 — CP0's z1.6 sits outside their 0.76 touchR, the w7l4 hut precedent) · teaching-
//   rank blasts are harmless lift · nearest REAL bite: MirrorFrost #1 hall min 44 - 0.66 = 43.3 at y4.6
//   (41u clear + a floor apart) · Carol #1 swoop min 53.4 - 0.66 = 52.7, dips to y5.6 (50u) · everything
//   else is farther and higher. IDLE-SAFE.
// THE LANTERN (x82, y9.4, z1.6, body 81.6..82.4): Mirror #2 hall 69..75, worst reach 75 + 0.66 = 75.66
//   (5.9u — and a mirror only moves while YOUR vx moves; an idle player freezes it) · icicle #4 danger
//   87.1..88.1 (4.7u) · Herald #1 x92 touch 91.32 (8.9u); beam spans 92.8..97.8, never left of 92.8
//   (10.4u) · icicle trio danger >=98.5 (16u) · wisp #1 lane max 58 on Loft 1 (24u + a floor) · wisp #2
//   lane min 104 (22u) · Carol #2 swoop max 64.6 (17u) · knights patrol >=121.4 on the choir (39u) ·
//   triplet lanes >=136.2 on the choir (fixed, cull-off) · crows are furniture. IDLE-SAFE.
//   (Kit law: a lantern's trigger is y-blind, so a NAVE walker under x82 can light it from below — but the
//   only people who walk the nave east past 82 are people who already fell FROM Loft 2 (the light is their
//   mercy), and abusing it fresh still costs a deliberate death to cash in. The court permits kindness.)
// FROSTCHEST (x15, y0 — CLEAR-PATCH, law asks >=6u): no patrol, beam, wake, lane, or lunge reaches the
//   nave floor ANYWHERE (herald beams live at y10.3/15.1 · knight lunges cap at 124.6+3.4+0.78=128.78 and
//   158.5+3.4+0.78=162.68, both at y14.2 · wisp wakes y>=6.1 · swoops floor at y5.6 · icicles fall onto
//   lofts y9.4/14.2 · triplet lanes y14.2, x>=136.2). Worst-case vertical clearance 4.6u, horizontal 28u+.
//   Its own flurry ambush (lanes +-10 at z0, span 2.4..27.6 — 2.8u short of pipe0's collider at 30.4) is
//   the kit's sanctioned, 1s-grace exception. Opening is a deliberate, safe act. CLEAR.
// SIGN CLEARANCES (a read is a breath, not a trap): x6 nave (all clear) · x72 Loft 2 lip — Mirror #2 hall
//   min 69 - 0.66 = 68.34, reader body 71.6..72.4 (2.9u; mirror is idle while you read) · x134.6 pomp —
//   Herald #2 beam ends 132.8 (reader body 134.2: 1.4u), roller lanes end >=137, worst bite edge
//   137 - 0.76 = 136.24 (1.2u; w7l4's 1.1 precedent) · x168.5 farewell — nothing lives on the porch. CLEAR.
// WORST SEAMS (<=4 law): x52-55 Loft 1 = mirror #1 + wisp #1 wake + Carol #1 swoop + the gap — 4, THE
//   cap, hit exactly once (the summit's one shouted chord) · x86-95 = icicle-over-gap + herald beam = 2-3 ·
//   icicle slalom = 1 dropping + 1 telegraphing = 2 · GREAT RANK ride = wisp wake + blast clocks (harmless
//   lift) = 2 · choir arrival = knight bow + herald phrase = 2-3 · arena worst = 2 rollers mid-lane +
//   knight #2's wake at the far edge = 3. The cap holds everywhere.
// GP idx1 — THE SILENT PIPE (x108): every case pipe glows on the rank beat; this one is dark and its gilt
//   plaque is BLANK (the observation read). Its cracked back panel — the split wraps the pipe's z-0.5 back
//   curve, read in depth from the loft — leaves a 1.5u opening at the left base (walk in), and the mouth
//   between the shoulder rims (gap 1.1) drops straight into the hollow (fall in). Gold light leaks from
//   the crack for whoever looks BACK. Never signposted. One visible-but-tricky (idx0) and one skill-gated
//   (idx2) live elsewhere in w10.
// ----------------------------------------------------------------------------------------------------

// ---- THE GREAT RANK — the kit organ's contract (inhale 0.7s glow+draw -> chord-note blast, vy 13,
// fixed slots on one period), re-anchored at a loft height (the kit builder is ground-anchored; level-
// local re-anchor per the w7l4Gantry precedent). Same timeline, same fairness, higher register. ----
function w10l3LoftRank(G, opts={}){
  const x=opts.x||0, n=opts.pipes||4, period=opts.period||4.8, phase=opts.phase||0, vy=opts.vy||13, baseY=opts.baseY||0;
  const NOTES=[330, 415, 523, 659, 784];   // a fifth above the nave rank — the ascent is audible
  const mouths=[];
  const deco=new THREE.Group();
  for(let i=0;i<n;i++){
    const px=x+i*1.7;
    const ph=2.2+((i*2)%3)*0.8;
    const pipe=mesh('cyl',[0.55,0.6,ph,10], emat(W10PAL.regalS,0x6a84b8,0.3)); pipe.position.set(px,baseY+ph/2,0); deco.add(pipe);
    const collar=mesh('cyl',[0.66,0.66,0.18,10], mat(W10PAL.regalG)); collar.position.set(px,baseY+ph-0.3,0); deco.add(collar);
    G.world.addBox(px, baseY, 0, 1.2, ph, 1.2, {});
    const mouthGlow=new THREE.Mesh(geo('cyl',0.45,0.45,0.08,10), new THREE.MeshBasicMaterial({color:W10PAL.frost, transparent:true, opacity:0.25, depthWrite:false}));
    mouthGlow.position.set(px, baseY+ph+0.06, 0); G.scene.add(mouthGlow);
    mouths.push({px, top:baseY+ph, glow:mouthGlow, slot:i});
  }
  G.scene.add(bakeGroup(deco));
  G.ents.add({ dead:false, cull:false, isEnemy:false, t:phase, group:new THREE.Group(), _fired:{},
    update(dt, GG){
      this.t+=dt;
      const cyc=this.t % period;
      const slotT=period/n;
      for(const m of mouths){
        const s0=m.slot*slotT;
        const inhale = cyc>=s0 && cyc<s0+0.7;
        const fireAt = s0+0.7;
        m.glow.material.opacity = inhale ? 0.3+Math.sin(this.t*22)*0.25 : 0.18;
        if(inhale && Math.random()<dt*10) GG.fx.spawn(new THREE.Vector3(m.px+rand(-0.5,0.5), m.top+rand(0.4,1.2), 0), 0xbfe8ff, 1, {speed:-0.5, life:0.35, gravity:-3});
        const key='p'+m.slot+'_'+Math.floor(this.t/period);
        if(cyc>=fireAt && cyc<fireAt+0.2 && !this._fired[key]){
          this._fired[key]=true;
          const pl=GG.player;
          if(pl && Math.abs(pl.pos.x-m.px)<20) AUDIO.tone && AUDIO.tone({f:NOTES[m.slot%NOTES.length], type:'triangle', t:0.7, vol:0.13});
          GG.fx.spawn(new THREE.Vector3(m.px, m.top+0.3, 0), 0xcfe4f4, 8, {speed:3, life:0.5, gravity:-2});
          if(pl && !pl.dead && pl.grounded && Math.abs(pl.pos.x-m.px)<0.75 && Math.abs(pl.pos.z)<0.9 && Math.abs(pl.pos.y-m.top)<0.5){
            pl.vel.y = vy; pl.grounded=false;
            pl._springAir = true;   // audit fix (kit parity): bypass the release-cut or the chord launch clamps to 6.5
            AUDIO.bounce && AUDIO.bounce();
          }
          if(Object.keys(this._fired).length>n*3) this._fired={};
        }
      }
    } });
  return mouths;
}

// ---- THE QUIET PROP: the organist's console and bench at the foot of the Instrument. Three tiered
// manuals, ivory keys dusted but not dirty (someone keeps them ready), a velvet bench with a dip worn
// into the cushion — one sitter, ten thousand years of practice — and on the stand a single sheet of
// music, set out neatly, its corners worn SOFT from being set out and put away, set out and put away.
// The piece is titled only "FOR WHEN THEY COME" (the title lives in the walk-up read; no signpost, no
// pointer — story-readers sit with it, everyone else walks past). Fully baked. ----
function w10l3Console(x, z){
  const g=new THREE.Group();
  const desk=mesh('box',[2.6,1.0,1.1], mat(0x3a2c50)); desk.position.set(x,0.5,z); g.add(desk);
  for(let i=0;i<3;i++){
    const kb=mesh('box',[2.0,0.09,0.4], mat(0xe8ecf4)); kb.position.set(x,1.05+i*0.22,z+0.5-i*0.16); g.add(kb);
    for(let k=0;k<7;k++){ if(k===2||k===6) continue;
      const bk=mesh('box',[0.12,0.06,0.2], mat(0x1a1a2e)); bk.position.set(x-0.82+k*0.27, 1.11+i*0.22, z+0.44-i*0.16); g.add(bk); }
  }
  for(const s of [-1,1]){ const cheek=mesh('box',[0.16,0.8,1.0], mat(W10PAL.regalG)); cheek.position.set(x+s*1.3,1.1,z+0.1); g.add(cheek); }
  // the music stand + THE SHEET
  const stand=mesh('box',[1.4,0.85,0.05], mat(0x3a2c50)); stand.position.set(x,2.0,z-0.25); stand.rotation.x=-0.28; g.add(stand);
  const sheet=mesh('box',[0.58,0.76,0.015], mat(0xf0ead8)); sheet.position.set(x,2.02,z-0.19); sheet.rotation.x=-0.28; g.add(sheet);
  // staves + the short title bar at the head of the page
  const title=mesh('box',[0.34,0.035,0.012], mat(0x4a4258)); title.position.set(x,2.33,z-0.275); title.rotation.x=-0.28; g.add(title);
  for(let l=0;l<4;l++){ const stave=mesh('box',[0.46,0.02,0.012], mat(0x8a8298));
    stave.position.set(x, 2.24-l*0.14, z-0.245-l*0.04); stave.rotation.x=-0.28; g.add(stave); }
  // the corners, worn soft — darker patches where the same two thumbs held it, forever
  for(const s of [-1,1]){ const worn=mesh('box',[0.1,0.09,0.018], mat(0xd6cba6)); worn.position.set(x+s*0.24,1.69,z-0.093); worn.rotation.x=-0.28; g.add(worn); }
  // the bench: velvet cushion with the sitter's dip
  const bench=mesh('box',[1.5,0.46,0.6], mat(0x4a3a60)); bench.position.set(x,0.23,z+1.25); g.add(bench);
  const cushion=mesh('box',[1.5,0.1,0.6], mat(W10PAL.velvet)); cushion.position.set(x,0.51,z+1.25); g.add(cushion);
  const dip=mesh('box',[0.56,0.045,0.44], mat(W10PAL.velvetD)); dip.position.set(x,0.545,z+1.25); g.add(dip);
  // a cold candle on the desk corner, burning its patient frost-flame
  const cand=mesh('cyl',[0.05,0.06,0.3,6], mat(W10PAL.regalS)); cand.position.set(x-1.05,1.15,z-0.3); g.add(cand);
  const fl=mesh('sph',[0.05,5,4], emat(0x7ae8ff,0x7ae8ff,0.9)); fl.position.set(x-1.05,1.36,z-0.3); g.add(fl);
  return g;
}

// ---- THE SILENT PIPE (GP idx1's home): a great stub pipe standing on Loft 2, dark on every beat, gilt
// plaque blank. Cutaway shell (solid back curve + a glassy front veil so the hollow reads), crack seam
// leaking gold at the left base, shoulder rims to cross, the mouth open between them. Baked visuals; the
// colliders are laid by the caller. ----
function w10l3SilentPipe(x, baseY){
  const g=new THREE.Group();
  const shellM=new THREE.MeshLambertMaterial({color:W10PAL.regalS, emissive:0x3a4a70, emissiveIntensity:0.12});
  const back=new THREE.Mesh(geo('cyl',1.15,1.25,3.0,16,1,true,Math.PI*0.62,Math.PI*1.76), shellM);
  back.position.set(x,baseY+1.5,0); g.add(back);
  const veil=new THREE.Mesh(geo('cyl',1.13,1.23,3.0,16,1,true,Math.PI*0.38-Math.PI*0.14,Math.PI*0.52),
    new THREE.MeshLambertMaterial({color:W10PAL.ice, transparent:true, opacity:0.16, depthWrite:false}));
  veil.position.set(x,baseY+1.5,0); g.add(veil);
  const rim=mesh('tor',[1.2,0.1,6,18], mat(W10PAL.regalG)); rim.rotation.x=Math.PI/2; rim.position.set(x,baseY+3.0,0); g.add(rim);
  const collar=mesh('cyl',[1.3,1.32,0.22,16], mat(0x2a3a60)); collar.position.set(x,baseY+0.35,0); g.add(collar);
  // the BLANK plaque — every sibling pipe wears a gilt number; this one was never given its cue
  const plaque=mesh('box',[0.42,0.3,0.05], mat(0x6a6f80)); plaque.position.set(x,baseY+2.2,1.18); g.add(plaque);
  // the crack — a thin gold-leaking seam wrapping from the back curve down to the left-base opening
  const seam1=mesh('box',[0.05,1.3,0.06], emat(0xd8b84a,0xb08a2a,0.8)); seam1.position.set(x-1.02,baseY+0.75,-0.5); seam1.rotation.z=0.18; g.add(seam1);
  const seam2=mesh('box',[0.05,0.8,0.06], emat(0xd8b84a,0xb08a2a,0.6)); seam2.position.set(x-0.78,baseY+1.6,-0.85); seam2.rotation.z=0.45; g.add(seam2);
  // dust circle inside — nothing has stirred in here since the tuning
  const dust=mesh('cyl',[0.95,0.95,0.03,14], mat(0x8a94ac)); dust.position.set(x,baseY+0.03,0); g.add(dust);
  return g;
}

// ---- LOFT SIGNPOST: the kit signPost plants at y0 (a loft's board would drown ten meters under its
// reader) — same pole+board language raised to the loft floor, baked into deco; the read trigger registers
// at the standard z1.7 lane offset (the prompt is y-blind kit law, which here only ever gifts a nave
// walker a preview of the court's manners). ----
function w10l3LoftSign(G, deco, x, y, ry, text){
  const g=new THREE.Group();
  const pole=mesh('cyl',[0.07,0.09,1.4,5], mat(PAL.woodD)); pole.position.y=0.7; g.add(pole);
  const board=mesh('box',[1.5,0.8,0.1], mat(PAL.wood)); board.position.y=1.5; crook(board,0.04); g.add(board);
  g.position.set(x,y,1.7); g.rotation.y=ry; deco.add(g);
  G.signs.push({x, z:1.7, text});
}

function buildW10L3(G){
  const S = G.scene;
  levelBegin(G);

  const FLOOR = W10PAL.iceD;                // palace marble-ice (grippy — precision never lives on slick)
  const deco = new THREE.Group();           // all static scenery bakes to one draw call at the tail

  // =============================== BEAT 1 — THE NAVE (x -8..28, y0) ===============================
  groundX(G, -8, 120, FLOOR);               // the nave floor runs UNDER the whole climb to the case plinth
                                            // at x120 — every fall west of the choir lands safe (re-climb law)
  G.ents.add(new Checkpoint(2, 0, 1.6, 0, {noLight:true}));   // CP0 — start (exam law: CP0 + ONE lit lantern)
  signPost(G, 6, 1.7, -0.12, "THE ORGAN LOFT. The Great Instrument has kept every rehearsal for ten thousand years. Each pipe DRAWS BREATH before it speaks - a glow, an inhale - and then the chord will LIFT whoever stands upon its mouth, most courteously, toward the registers above.");
  candyLine(G, [[5,0.9,0],[7.5,0.9,0],[10,0.9,0]], 3);
  // THE QUIET PROP (never signposted): the console, the bench, and the sheet that waits — walk-up read only
  deco.add(w10l3Console(11, -1.4));
  G.signs.push({x:11, z:0, text:'A single sheet of music waits on the stand, set out neatly, the way it is set out every night. The corners are worn soft as cloth - set out and put away, set out and put away, ten thousand times. The piece is titled only: "FOR WHEN THEY COME."'});
  // THE FROSTCHEST — the gamble, in its CLEAR POCKET (CLEAR-PATCH law — full reach math in the ledger:
  // nothing bites at y0 anywhere; its own flurry ambush is the kit's grace-protected exception)
  { const c = new FrostChest(15, 0, -0.9, 0.25); G.coffins.push(c); G.ents.add(c); }
  G.ents.add(new Crow(20, 0.95, 2.1));      // palace crow #1 — unimpressed by the acoustics

  // =============================== BEAT 2 — THE TEACHING RANK (x 28..39): INTRODUCE the blast ===============================
  // The console stair boards pipe0; the inhale (0.7s) is the telegraph; the blast is harmless LIFT.
  // Pipes px 31/32.7/34.4/36.1, tops 2.2/3.8/3.0/2.2 — hop the rank or ride it; pipe1's blast (apex 7.3)
  // crowns Loft 1's lip (4.6) with +2.7 to spare, pipe3's (5.7) clears it with +1.1. Candy traces both.
  { const s1=mesh('box',[1.4,0.9,1.6], mat(W10PAL.regalS)); s1.position.set(28.2,0.45,0); deco.add(s1);
    const s2=mesh('box',[1.2,1.8,1.6], mat(W10PAL.regalS)); s2.position.set(29.6,0.9,0); deco.add(s2);
    const trim=mesh('box',[1.2,0.08,1.7], mat(W10PAL.regalG)); trim.position.set(29.6,1.84,0); deco.add(trim); }
  G.world.addBox(28.2, 0, 0, 1.4, 0.9, 1.6, {});
  G.world.addBox(29.6, 0, 0, 1.2, 1.8, 1.6, {});
  w10Organ(G, {x:31, pipes:4, period:4.8, phase:0, vy:13});
  candyLine(G, [[31,3.2,0],[32.7,4.8,0],[36.1,3.2,0]], 3);    // the pipe tops — where the lift lives
  candyLine(G, [[37.2,6.0,0],[39,5.6,0]], 2);                 // the launch arc onto the loft

  // =============================== BEAT 3 — LOFT 1 (x 39..70, y4.6): TWIST — the loft air ===============================
  // Three slabs, two tap-gaps. The ONE cap seam (x52-55): mirror + wisp wake + Carol #1's swoop + the gap
  // = 4 systems, all telegraphed, all on fixed clocks — the summit's single shouted chord.
  platform(G, 45.5, 4.6, 0, 13, 4.5, FLOOR);                  // slab 1: x39..52
  platform(G, 57.5, 4.6, 0, 5, 4.5, FLOOR);                   // slab 2: x55..60
  platform(G, 66.7, 4.6, 0, 6.6, 4.5, FLOOR);                 // slab 3: x63.4..70
  // THE MERCY RUNNER (x38.6) — a velvet runner spills over slab 1's WEST lip to the nave floor: any fall
  // re-climbs HERE instead of re-walking the whole rank (it lands you at Loft 1's start — no exam skipped,
  // the slow-safe line by design). It hangs BESIDE the lip, never through it (volume 38.1..39.1 vs the
  // slab's solid starting x39 — the climber's 0.35 body never kisses it); top-out 5.4 is above the 4.6
  // deck, boosted hop right to board (the w5Chain idiom). Mirror #1's bite starts 43.3 — a clear landing.
  { for(const s of [-1,1]){
      const pan=mesh('box',[0.9,5.2,0.06], mat(s<0?W10PAL.velvet:W10PAL.velvetD)); pan.position.set(38.6+s*0.02,2.62,-0.35-s*0.04); deco.add(pan);
    }
    const fr=mesh('box',[0.94,0.12,0.08], mat(W10PAL.regalG)); fr.position.set(38.6,0.08,-0.35); deco.add(fr);
    const rod2=mesh('cyl',[0.06,0.06,1.4,7], mat(W10PAL.regalG)); rod2.rotation.z=Math.PI/2; rod2.position.set(38.9,5.32,-0.35); deco.add(rod2); }
  G.world.addBox(38.6, 0, 0, 1.0, 5.4, 1.2, {type:'climb'});
  // velvet runners down the slab centers (the court keeps its carpets, even here)
  for(const [rc,rw] of [[45.5,12.4],[57.5,4.4],[66.7,6.0]]){
    const run=mesh('box',[rw,0.05,1.2], mat(W10PAL.velvet)); run.position.set(rc,4.64,0); deco.add(run);
    const hem=mesh('box',[rw,0.03,1.3], mat(W10PAL.regalG)); hem.position.set(rc,4.62,0); deco.add(hem);
  }
  // MIRROR FROST #1 — the hall x44..54: it mirrors your x-velocity reversed. Feint AWAY, then run past.
  G.ents.add(new MirrorFrost(G, 49, 4.6, 0, {phase:0.4, range:5, mirror:1.0}));
  // AURORA WISP #1 — swims x44..58 at y6.9: the wake bites 0.8s then FADES — cross the sky on the fade.
  // A walker's head (5.8) stays under the band (6.1+); a JUMPER threads it — time the gap hops.
  G.ents.add(new AuroraWisp(G, {x0:44, x1:58, y:6.9, period:6.5, amp:0.8, phase:1.2, color:0x58e0a8}));
  // THE CAROL DUET — verses 0 and 2 of a three-voice round, period 6 (the round the palace practices with
  // an empty chair where the middle voice should stand). Verse 1's silence (cyc 2..4) is the crossing
  // window; the stare gives stage fright; the swoops dip to y5.6 — heads down or windows read.
  G.ents.add(new CarolBoo(G, 56, 7.2, 0, {verse:0, period:6, range:9}));
  G.ents.add(new CarolBoo(G, 62, 7.2, 0, {verse:2, period:6, range:9}));
  candyLine(G, [[42,5.5,0],[46,5.5,0],[50,5.5,0]], 3);        // the low walk-line under the ribbon
  candyLine(G, [[52,5.6,0],[53.5,6.4,0],[55,5.6,0]], 3);      // gap 1, arced (3.0 tap)
  candyLine(G, [[60,5.6,0],[61.7,6.4,0],[63.4,5.6,0]], 3);    // gap 2, arced (3.4 tap) — under verse 2
  candyLine(G, [[65,7.8,0],[67,8.8,0]], 2);                   // the EXPERT wink: bounce the swoop to the lip
  G.ents.add(new Crow(46, 5.55, 2.1));                        // palace crow #2 — has heard the round before
  // THE TAPESTRY CLIMB (x67): velvet runners hung from Loft 2's rail — climb volume 4.6->10.6, boosted
  // hop off the top onto the 9.4 lip. The climb rhythm is candy-traced.
  { const rod=mesh('cyl',[0.07,0.07,2.6,7], mat(W10PAL.regalG)); rod.rotation.z=Math.PI/2; rod.position.set(67,10.8,-0.35); deco.add(rod);
    for(const s of [-1,1]){
      const pan=mesh('box',[0.92,6.1,0.06], mat(s<0?W10PAL.velvet:W10PAL.velvetD)); pan.position.set(67+s*0.02,7.7,-0.35-s*0.04); deco.add(pan);
      const fringe=mesh('box',[0.96,0.12,0.08], mat(W10PAL.regalG)); fringe.position.set(67+s*0.02,4.62,-0.35-s*0.04); deco.add(fringe);
    }
    const crest=mesh('tor',[0.2,0.04,4,10], mat(W10PAL.regalG)); crest.rotation.x=Math.PI/2; crest.position.set(67,10.95,-0.35); deco.add(crest); }
  G.world.addBox(67, 4.6, 0, 1.0, 6.0, 1.2, {type:'climb'});
  candyLine(G, [[67,6.2,0],[67,8.0,0],[67,9.8,0]], 3);

  // =============================== BEAT 4 — LOFT 2 (x 68..119.5, y9.4): the lantern, then the ESCALATE ===============================
  platform(G, 77, 9.4, 0, 18, 4.5, FLOOR);                    // slab 1: x68..86
  platform(G, 104.35, 9.4, 0, 30.3, 4.5, FLOOR);              // slab 2: x89.2..119.5 (gap 3.2 between)
  for(const [rc,rw] of [[77,17.4],[104.35,29.6]]){
    const run=mesh('box',[rw,0.05,1.2], mat(W10PAL.velvet)); run.position.set(rc,9.44,0); deco.add(run);
  }
  w10l3LoftSign(G, deco, 72, 9.4, -0.1, "The Second Loft. The Court Herald rehearses his counterpoint across this landing: the horn comes up - a gleam, a rising third - and then the winter itself is BLOWN along the phrase. One hops the bar line. One does not critique it.");
  // MIRROR FROST #2 — the tighter hall (x69..75, mirror 1.15): the arrival's manners test, feint-taught upstairs
  G.ents.add(new MirrorFrost(G, 72, 9.4, 0, {phase:1.1, range:3, mirror:1.15}));
  // THE lantern — the level's ONE lit checkpoint (x82 of the -8..166 walk ≈ 52%). Ledger says IDLE-SAFE.
  G.ents.add(new Checkpoint(82, 9.4, 1.6, 1));
  G.ents.add(new BonkLantern(G, 84.3, 10.9, 0, 'shield'));    // armor before the exam's second page
  candyLine(G, [[70.5,10.3,0],[74,10.3,0]], 2);
  candyLine(G, [[80.5,10.3,0]], 1);
  // ICICLE #4 — over the loft gap: the vault weeps on a fixed clock; hop the 3.2 gap on the shimmer's rest
  G.ents.add(new SpikeIcicle(G, 87.6, 14.4, {period:4.6, phase:1.1, len:1.2, floorY:9.4}));
  candyLine(G, [[86,10.4,0],[87.6,11.2,0],[89.2,10.4,0]], 3); // the gap arc, timed against the drip
  // FROST HERALD #1 — the counterpoint landing (x92, dir +1): beam 92.8..97.8 at y10.3 on a 5.2s clock.
  // Hop the phrase (feet over 10.65) or spend the horn-raise walking. Stompable — silencing him is legal.
  G.ents.add(new FrostHerald(G, 92, 9.4, 0, {phase:0.6, period:5.2, reach:4.2, dir:1}));
  candyLine(G, [[93.5,10.5,0],[95.5,10.5,0]], 2);
  // THE ICICLE SLALOM — three staggered clocks under the case arch (1 dropping + 1 telegraphing at worst)
  { const arch=mesh('box',[8.4,0.6,2.6], mat(0x2a3a60)); arch.position.set(101.5,15.1,-0.4); deco.add(arch);
    for(const ax of [98.2,104.8]){ const ped=mesh('cyl',[0.3,0.4,5.4,7], mat(0x2a3a60)); ped.position.set(ax,12.2,-1.5); deco.add(ped); } }
  G.ents.add(new SpikeIcicle(G, 99, 14.8, {period:4.4, phase:0.0, len:1.2, floorY:9.4}));
  G.ents.add(new SpikeIcicle(G, 101.5, 14.8, {period:4.8, phase:1.6, len:1.2, floorY:9.4}));
  G.ents.add(new SpikeIcicle(G, 104, 14.8, {period:5.2, phase:3.2, len:1.2, floorY:9.4}));
  candyLine(G, [[100.2,10.3,0],[102.7,10.3,0]], 2);           // the slalom line — between the drop columns
  // THE SILENT PIPE (x108) — GP idx1 inside. Flange steps 1.5/1.5 cross its shoulders (rims y12.4); the
  // mouth gap (107.6..108.7) drops in; the cracked back panel (left base, 1.5u opening) walks in and out.
  deco.add(w10l3SilentPipe(108, 9.4));
  { const st=mesh('box',[1.3,1.5,1.6], mat(W10PAL.regalS)); st.position.set(105.9,10.15,0); deco.add(st);
    const st2=mesh('box',[1.3,1.5,1.6], mat(W10PAL.regalS)); st2.position.set(110.4,10.15,0); deco.add(st2); }
  G.world.addBox(105.9, 9.4, 0, 1.3, 1.5, 1.6, {});           // flange L (top 10.9)
  G.world.addBox(107.15, 10.9, 0, 0.9, 1.5, 2.0, {});         // left shoulder (top 12.4; the crack = the open 9.4..10.9 below)
  G.world.addBox(109.15, 9.4, 0, 0.9, 3.0, 2.0, {});          // right wall, full height (top 12.4)
  G.world.addBox(110.4, 9.4, 0, 1.3, 1.5, 1.6, {});           // flange R (top 10.9)
  G.ents.add(new GoldPumpkin(108.15, 10.6, 0, 1));            // GP idx1 — its gold leaks through the crack
  G.ents.add(new Heart(111.9, 11.3, 0));                      // mercy before the high rank

  // =============================== BEAT 5 — THE GREAT RANK (x 111..120): MASTER pt.1 — the chord ascent ===============================
  // Loft-anchored rank (baseY 9.4): tops 11.6/13.2/12.4/11.6, blasts crest 15.1..16.7 over the choir lip
  // (14.2). AuroraWisp #2's ribbon swims the rank air — thread the wake between chords. The FAST line.
  { const st=mesh('box',[1.4,1.1,1.6], mat(W10PAL.regalS)); st.position.set(111.6,9.95,0); deco.add(st); }
  G.world.addBox(111.6, 9.4, 0, 1.4, 1.1, 1.6, {});           // rank stair (top 10.5)
  w10l3LoftRank(G, {x:113, baseY:9.4, pipes:4, period:4.8, phase:2.4, vy:13});
  G.ents.add(new AuroraWisp(G, {x0:104, x1:118, y:12.6, period:7, amp:0.8, phase:3.4, color:0xb08aff}));
  G.ents.add(new BonkLantern(G, 114.7, 15.6, 0, 'bat'));      // the crown prize over pipe1 — wings for the choir
  candyLine(G, [[113,12.6,0],[114.7,14.2,0],[117,13.4,0],[119.5,14.6,0]], 4);   // the chord ride, register by register
  // THE BELL-ROPE (the patient line, x119.6): w5Chain 10.0->15.2, boosted hop onto the lip — the rank's
  // candy halo hangs in plain sight from the rope (the "next run I'm riding the chord" itch)
  w5Chain(G, 119.6, 10.0, 15.2, 0);
  candyLine(G, [[119.6,11.5,0],[119.6,13.5,0]], 2);

  // =============================== BEAT 6 — THE CHOIR FLOOR (x 120..163, y14.2): arrival + MID-BOSS ===============================
  platform(G, 141.5, 14.2, 0, 43, 6, FLOOR);
  { const run=mesh('box',[42,0.05,1.4], mat(W10PAL.velvet)); run.position.set(141.5,14.24,0); deco.add(run);
    const lip=mesh('box',[43,0.08,6.2], mat(0xc4d4ea)); lip.position.set(141.5,14.21,0); deco.add(lip); }
  // THE WEST PLINTH — the great case's foot (the level's spine made honest): the nave DEAD-ENDS against it,
  // so the only way east is up and over the Instrument (w8l3 split-floor law). Solid x120.0..121.6, y0..13.7,
  // flush under the choir's underside; rank blasts cross its top with >=0.5 to spare at full airspeed (more
  // when slower — the arc peaks later), and the bell-rope column (119.15..120.05) hangs clear of its face.
  { const wallW=mesh('box',[1.6,13.7,3], mat(W10PAL.wall)); wallW.position.set(120.8,6.85,0); deco.add(wallW);
    const trimW=mesh('box',[1.7,0.3,3.1], mat(W10PAL.regalG)); trimW.position.set(120.8,13.4,0); deco.add(trimW);
    const crest2=mesh('cone',[0.3,0.7,6], emat(W10PAL.frost,W10PAL.frost,0.5)); crest2.position.set(120.8,2.2,1.6); deco.add(crest2); }   // a gilt case-crest at nave eye level — the dead-end reads as furniture, not error
  G.world.addBox(120.8, 0, 0, 1.6, 13.7, 3, {});
  // FROST KNIGHT #1 — the courtly greeting (patrol 121.4..124.6): the bow IS the telegraph, learned in the
  // Keep long ago. His wake covers the arrival lip — you land with airtime enough to read the courtesy.
  G.ents.add(new FrostKnight(G, 123, 14.2, 0, {phase:0.2, range:1.6, dir:1, speed:1.5}));
  // FROST HERALD #2 — the second phrase (x127, dir +1): beam 127.8..132.8 at y15.1, period 4.8
  G.ents.add(new FrostHerald(G, 127, 14.2, 0, {phase:2.1, period:4.8, reach:4.2, dir:1}));
  candyLine(G, [[121.5,15.1,0],[129.5,15.2,0],[131.5,15.2,0]], 3);
  // THE POMP SIGN (mandated wording — the court's whole relationship with the trio in four sentences)
  w10l3LoftSign(G, deco, 134.6, 14.2, -0.1, "The Triplets. Yes. Even here. The court has learned not to remark upon it.");
  // MID-BOSS: THE FLURRY TRIPLETS — three lanes in staggered thirds (period 5.1, phases 0/1.7/3.4), all
  // rolling head-on, fixed spans, fixed clocks (cull:false — the shared round NEVER de-syncs, CarolBoo
  // law). Stomp each once while the others roll, or dance the thirds and keep your appointment upstairs.
  // (t3's shorter lane keeps the SAME 5.1s period via its longer rest — 15.5/4.4 + 1.577 = 5.1 exactly,
  // matching t1/t2's 16.5/4.4 + 1.35: equal periods are what KEEP the thirds staggered forever)
  { const t1=new FlurryTriplet(G, 154, 14.2, 0, {x1:137.5, speed:4.4, phase:0.0, pause:1.35}); t1.cull=false; G.ents.add(t1);
    const t2=new FlurryTriplet(G, 155.5, 14.2, 0, {x1:139, speed:4.4, phase:1.7, pause:1.35}); t2.cull=false; G.ents.add(t2);
    const t3=new FlurryTriplet(G, 152.5, 14.2, 0, {x1:137, speed:4.4, phase:3.4, pause:1.577}); t3.cull=false; G.ents.add(t3); }
  candyLine(G, [[139,15.1,0],[143,15.1,0],[147.5,15.1,0],[151,15.1,0]], 4);   // the dodge rhythm, traced
  // THE CHOIR-LAMP PERCH — the refuge (y16.7, double-jump 2.5 rise, candy-traced): a hanging dish the
  // rollers pass cleanly under (their crowns top out at 15.8; the dish floor is 16.2)
  platform(G, 145.6, 16.7, 0, 1.6, 1.8, W10PAL.regalG);
  { const chainV=mesh('cyl',[0.04,0.04,3.2,5], mat(0x2a3a60)); chainV.position.set(145.6,18.4,0); deco.add(chainV);
    const lamp=mesh('sph',[0.22,7,6], emat(0x7ae8ff,0x7ae8ff,0.7)); lamp.position.set(145.6,16.95,0); deco.add(lamp); }
  candyLine(G, [[145.6,17.3,0]], 1);
  // FROST KNIGHT #2 — the last watch (patrol 155.5..158.5; lunge caps at 162.68, inside the floor's 163 edge)
  G.ents.add(new FrostKnight(G, 157, 14.2, 0, {phase:1.4, range:1.5, dir:-1, speed:1.6}));
  candyLine(G, [[161,15.1,0]], 1);

  // =============================== BEAT 7 — THE RECESSIONAL (x 163.8..176): down the case, and out ===============================
  // THE EAST PLINTH first — the case's east flank: seals the under-choir cavity and carries the floor lip
  // out to 163.8 at full height (a seamless last step). Then the porch: cornice ledges step down the flank
  // (drops 3.4/3.6/3.6/3.6 — down is always free; every miss lands on porch ground), the long bell-rope
  // down the plinth face is the careful line AND the climb back up for anything left behind. The porch is
  // only ever entered from above — the walk-to-win seal's other bookend.
  { const wallE=mesh('box',[1.4,14.2,3], mat(W10PAL.wall)); wallE.position.set(163.1,7.1,0); deco.add(wallE);
    const trimE=mesh('box',[1.5,0.3,3.1], mat(W10PAL.regalG)); trimE.position.set(163.1,13.9,0); deco.add(trimE); }
  G.world.addBox(163.1, 0, 0, 1.4, 14.2, 3, {});              // floor-flush top: the choir's last stride ends at 163.8
  groundX(G, 163.8, 176, FLOOR);                              // the porch — gate ground, reachable only from above
  w5Chain(G, 164.25, 0.4, 13.8, 0);                           // the great bell-rope down the east flank (volume 163.8..164.7, flush to the plinth face, clear of ledge 1 at 164.8)
  platform(G, 165.6, 10.8, 0, 1.6, 2.4, W10PAL.regalS);
  platform(G, 167.2, 7.2, 0, 1.6, 2.4, W10PAL.regalS);
  platform(G, 168.8, 3.6, 0, 1.6, 2.4, W10PAL.regalS);
  for(const [px,py] of [[165.6,10.3],[167.2,6.7],[168.8,3.1]]){
    const col=mesh('cyl',[0.14,0.2,py,7], mat(0x2a3a60)); col.position.set(px,py/2,-1.5); deco.add(col);
  }
  candyLine(G, [[165.6,11.7,0],[167.2,8.1,0],[168.8,4.5,0]], 3);
  signPost(G, 168.5, 1.7, -0.1, "The Recessional. The Instrument thanks you for your attendance, and will hold your chord until you call again. It has been - and the court does not say this lightly - a very long intermission.");
  candyLine(G, [[169.5,0.9,0],[170.3,0.9,0]], 2);
  { const lamp = new THREE.PointLight(0xffb85e, 24, 10); lamp.position.set(171.5, 3.4, -1); S.add(lamp); }   // the gate's warm welcome
  exitGate(G, 171);

  // =============================== DECO · THE GREAT CASE · SILHOUETTES · TAIL ===============================
  // the Instrument's case behind the climb — ranked background pipes rising with the lofts, mouths faintly
  // lit (baked emissive; the SILENT pipe up on Loft 2 is the one dark throat in the whole wall)
  for(const [bx,bh] of [[74,7],[79,9],[84,8],[90,11],[96,10],[102,13],[112,15],[116,12],[126,16],[136,15],[146,17],[156,16]]){
    const bp=mesh('cyl',[0.7,0.8,bh,9], mat(0x2a3a60)); bp.position.set(bx,bh/2,-2.9); deco.add(bp);
    const bm=mesh('cyl',[0.5,0.5,0.1,9], emat(0x58e0a8,0x58e0a8,0.35)); bm.position.set(bx,bh+0.06,-2.9); deco.add(bm);
    const bc=mesh('sph',[0.09,5,4], emat(W10PAL.regalG,W10PAL.regalG,0.7)); bc.position.set(bx,bh*0.6,-2.15); deco.add(bc);   // the gilt number every pipe wears — except one
  }
  // loft balustrades (baked, no collider — the rails read the edges)
  for(const [b0,b1,by] of [[39.5,51.5,4.6],[55.5,59.5,4.6],[63.9,66.3,4.6],[68.5,85.5,9.4],[89.7,105,9.4]]){
    for(let bx=b0; bx<=b1; bx+=2.4){ const post=mesh('cyl',[0.05,0.07,0.75,5], mat(0x2a3a60)); post.position.set(bx,by+0.37,-1.95); deco.add(post); }
    const rail=mesh('box',[b1-b0+0.4,0.08,0.1], mat(W10PAL.regalG)); rail.position.set((b0+b1)/2,by+0.78,-1.95); deco.add(rail);
  }
  // FOREGROUND silhouettes (z>0): fallen pipe sections + pew backs framing the depth
  for(const [fx,fr] of [[24,0.3],[58,-0.25],[95,0.35],[130,-0.3],[160,0.25]]){
    const fp=mesh('cyl',[0.5,0.55,2.6,8], mat(0x121c34)); fp.position.set(fx,0.5,2.7); fp.rotation.z=Math.PI/2+fr; deco.add(fp);
  }
  for(const px2 of [-4,40,118]){ const pew=mesh('box',[2.8,1.1,0.3], mat(0x161226)); pew.position.set(px2,0.55,2.5); deco.add(pew); }
  S.add(bakeGroup(deco));

  // the winter moon through the great rose (the kit parallax hangs the windows; the moon keeps its post)
  const moon = mesh('circ',[3.6,28], emat(0xe4ecff, 0xcfdcf6, 0.85)); moon.position.set(90, 17, -30); S.add(moon);

  w10Parallax(S, -8, 176);
  w10LevelFinish(G, -8, 176, 'palace');

  return {spawnX: 0, exitX: 171};
}

function updateW10L3(G, dt){
  updateLevelCommon(G, dt);
}

W10_LEVELS.push({id:'w10l3', district:'w10', name:'THE ORGAN LOFT', build:buildW10L3, update:updateW10L3, parTime:190});
