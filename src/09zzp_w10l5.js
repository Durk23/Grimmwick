// ============ LEVEL 10-5 — THE THRONE APPROACH (District w10 · The Aurora Palace · where the cold sits crowned) ============
// THE FINAL EXAM OF THE WHOLE GAME. Winter-finale band (the summit past D5) — and still MAIN-GAME FAIR:
// hearts-always, telegraphs >=0.6s, <=4 simultaneous threat systems, fixed clocks, one-good-run-away, never
// Kaizo. Everything the palace taught, composed: geysers under herald fire → the calendar wall threaded
// through flurry lanes → the honor-guard relay over the void → THE STILL COURT (the guest chair — the
// district warp; stillness is the gate) → the last gauntlet (knights + mirrors + the court's one surviving
// carol) → the great doors. EXACTLY CP0 (noLight) + ONE lit lantern at x104 (54.9% of the -8..196 run).
// 16 threats: 3 FrostKnight + 2 FrostHerald + 2 MirrorFrost + 2 AuroraWisp + 3 CarolBoo (reuse #1, trio) +
// 2 FlurryTriplet + 2 SpikeIcicle (reuse #2) — plus 4 nutcracker soldier MOVERS (platforms, never a bite).
// Golden Pumpkin idx 2 — skill-gated (the tallest geyser → the last organ pipe's chord → the rose-window
// ledge). NO Leap of Faith (both of the game's two are placed and sacred). SPEEDRUN NORTH STAR: every
// machine has a fast line (pinned per beat below); candy traces the racing line end to end.
//
//   BEAT 1 THE PROCESSION ROAD (breath)   x -8..44    — CP0 (noLight), the exam sign, the FrostChest gamble
//          in its clear pocket, the velvet processional. Nothing bites west of x53.
//   BEAT 2 THE FOUNTAIN COURT (COMPOSE 1) x 44..76    — the sunken fountain plaza: geyser A is the ONLY way
//          out of the recess (10u, mover-gated); herald #1 sweeps the ledge lane on the SAME 5.6s clock;
//          geyser B lifts to the gallery (icicle #1 walk-dodge) or drop east for the low road underneath.
//          FAST LINE: ride A on its 3.2s beat, stomp the herald, B straight up — or skip the gallery low.
//   BEAT 2b THE LAST PIPE (GP, expert)    x 76..86    — the tallest geyser (cap 5.0) → hop DOWN onto the
//          one surviving organ pipe (mouth 2.2) → its 4.8s chord fires vy16 (~+5.8) → rose-window ledge
//          y6.6, GP idx 2 at y7.5. Candy traces cap→mouth. Low-roaders who hop the pipe off-beat get
//          harmlessly LAUNCHED — and learn the route.
//   BEAT 3 THE CALENDAR WALL (COMPOSE 2)  x 86..102   — the advent wall at x98 (two stacked doors, ONE 8.4s
//          calendar: floor door open 0..2.8, all shut 2.8..5.6, high door 5.6..8.4; numbers curtsy 0.7s
//          before closing) threaded during two flurry lanes on a LOCKED 4.2s half-clock + icicle #2 over
//          the deep pocket. FAST LINE: arrive on the floor-door window and never stop; the chain climb to
//          the high door is the patient line (its west stub is a flurry-free perch).
//   BEAT 4 THE LANTERN LANDING (breath)   x 102..113.4 — THE lit lantern (x104, 54.9%), a shield lantern,
//          the parade sign. A true breath (ledger below).
//   BEAT 5 THE SOLDIER PARADE (COMPOSE 3) x 113.4..134.2 — four nutcrackers march the void in perfect relay
//          (ONE 4s beat, meets at shared endpoints): board S1 west-end on the beat, change hats at every
//          meeting, 8.0s lip-to-lip with ZERO waiting on a clean read. Aurora wisp #1 ribbons between the
//          roads; the tapestry climb boards the chandelier HIGH ROAD (heart + candy, wisp-contested).
//   BEAT 6 THE STILL COURT (the warp)     x 134.2..158.8 — the antechamber. ThroneShadow at x146 under its
//          lone purple lantern: SIT three uninterrupted seconds while herald #2 (7.8s horn) and wisp #2
//          (7.8s lane) sweep the hall. THE WINDOW IS HONEST (math below): two fat safe windows per 15.6s.
//   BEAT 7 THE LAST GAUNTLET (MASTER)     x 158.8..188 — knights bow, mirrors mirror, and the CarolBoo trio
//          sings the palace's only complete round (verses 0/1/2 — the court kept ONE song alive). Two
//          dressed gaps bracket it. FAST LINE: stare a verse silent, feint the mirrors, never stop moving.
//   BEAT 8 THE GREAT DOORS (exhale)       x 188..196  — the cracking end sign, the coat rack with the one
//          small empty hook, the gate. The seat was always kept.
//
// ROUTES (2-3 visible, junctions sighted): LOW = recess → under-gallery floor → floor door → the hat relay ·
// HIGH = geyser B gallery (icicle + candy + the GP sightline: from the gallery end you SEE the tall geyser
// and the rose-window candy — the "next run" itch) → chain climb high door → tapestry chandelier road ·
// EXPERT = the GP chord line + the guest chair itself (the warp to x191, full candy bonus).
//
// REACH MATH (jump model: tap 1.8 / held 2.6 / double 3.3 · gaps <=4 tap, <=5.5 held · geysers/organ/chain/
// soldiers gate the rest — all over-cleared, never exact):
//   recess drop-in 1.8 (free); back out west 1.8 held · geyser A cap 3.0 → ledge 2.8 (dx 2.8, drop 0.2) ·
//   beam hop on ledge: clear y4.2 = jump 1.4 (tap 1.8 over-clears) · geyser B cap 5.4 → gallery 5.2 (dx 1.4,
//   drop 0.2) · gallery → step 3.4 → ground (drops, free) · tall cap 5.0 → pipe mouth 2.2 (dx 2.6 down) ·
//   chord vy16 ≈ +5.8 from 2.2 → apex ~8.0, drift 2.6 east → ledge 6.6 (over-clear 1.4) · organ pipe as a
//   ground step: 2.2 = the main-step law limit (held 2.6 with margin) · chain top 6.2 → sill 3.4 (dx 1.0
//   boosted hop); wall crown 10 beats chain+double 9.5 (no over-the-top) · door headrooms 2.3 both ·
//   parade board rise 1.2/dx 0.55 · transfers are WALK-ACROSS at hat meets (gap <=0.4 + overlap) · exit
//   drop 1.2/dx 0.95 · tapestry top 4.6 → P1 4.2 (dx 2.9) · high-road gaps 3.0/3.0/3.1, rises <=0.5 ·
//   gap #1 3.5 (tap law) · gap #2 3.8 (tap law) · GP bypass audit: ground double 3.3 < ledge 6.6 (blocked);
//   tall-cap 5.0 + double = 8.3 over dx 5.2 lands it too — same skill family, allowed and documented.
// DETERMINISM: every machine on a fixed clock from level start — geyser A/B period 5.6 phases 0/2.8 · herald
// #1 period 5.6 phase 4.4 (beam window t≡0.4..1.4 — geyser A's ride window 3.2..4.95 is ALWAYS clear) · tall
// geyser 6.4 · organ 4.8 (fires at cyc 0.7) · wall 8.4 · flurries 4.2/phases 0,2.1 (LOCKED half of the wall
// clock — beat 3 is one composite machine) · icicles 4.4/4.2 · soldiers speed 2.4 span 4.8 (P 2.0) phases
// 0,2,0,2 · wisps 6.5/7.8 · herald #2 7.8 phase 1.2 · carol trio one 6.9s round, verses 0/1/2 · knights fixed
// phases (bow is player-triggered — reactive by design, like every patroller). Mirrors are player-velocity
// driven CLAMPED TO FIXED HALLS (kit contract — the player IS their clock). rand() only inside baked deco.
// CULL LEDGER (the w9l5 CarolBoo lesson): clock-COUPLED pieces must never freeze at 70u or their promised
// phase relationships drift with approach time — cull=false forced on: both FlurryTriplets (locked to the
// wall), herald #2 (locked to wisp #2's window). Heralds #1 sits 56u from spawn (never culled in practice)
// but is forced too, for the geyser-A promise. Geysers/icicles/wisps/carols/wall/organ tickers ship
// cull:false in their kits. Knights/mirrors stay cullable (reactive, promise-free).
// THREAT-SYSTEM CAP (<=4): B2 geyser clocks + herald#1 + icicle#1 = 3 · B3 wall + flurry lane 1 + flurry
// lane 2 + icicle#2 = 4 (the exam's one full chord, all four on one composite clock) · B5 soldier relay
// (platform) + wisp#1 = 2 (+the void) · B6 herald#2 + wisp#2 + the stillness itself = 3 · B7 knights +
// mirrors + carols = 3 (worst pinch x177.5..181.5: K3 + mirror#2 + carol#3 = 3; K2's lunge tip 180.98 can
// make a 3 with mirror#2 + carol#3 — K2 is then PAST its own hall, never a 4th system in one pocket).
// HEARTS ALWAYS: beams/wakes/lunges/swoops/flurries/icicles cost exactly 1 · both dressed gaps and the
// parade void are the kit's full pit price (heart + lantern walk-back) · geyser shatters, organ chords and
// shut doors are HARMLESS (a door closing on you ejects you along x — never a crush, never a hit) · the
// broken sit costs only the sit (unless what broke it was a beam — 1 heart, as telegraphed).
//
// SPAWN SAFETY (idle at x0/z0, CP0 respawn x2/z1.6): nearest FIXED bite — herald #1 beam min reach
// 58-0.8-4.2 = 53 and it lives at y3.5..4.2 (51u clear, high) · herald #1 touch home x58 (56u) · flurry #1
// touch min 83-0.8-0.42 = 81.8 (79.8u) · icicle #1 x67.5±0.5 (65u) · icicle #2 x92.6±0.5 (90.1u) · wisp #1
// wake min 115-0.5 (112.5u) · soldiers harmless · carol swoop min 164-2.6-0.66 = 160.7 · knight worst west
// 166-3.4-0.78 = 161.8 · mirror halls >=162.5 · herald #2 beam min 145.4 · geysers/organ/doors harmless.
// FrostChest ambush (x16, opt-in, player-opened): four flurries on the kit's fixed ±2.6/±1.4 ring, lanes
// ±10 at z0 with the 1s spawnGrace — CP0's z1.6 sits outside their 1.18 touch reach (the w7l4 z-offset
// law). All clear by 50u+.
// LANTERN SAFETY (idle at x104/z1.6, THE lit lantern): flurry #2 touch max 93.6+1.18 = 94.78 (9.2 clear,
// lanes fixed) · icicle #2 reach 93.1 (10.9) · wall doors harmless · wisp #1 wake min 114.5 (10.5) · the
// parade void lip 113.4 is an opt-in step (9.4) · everything east bites no closer than 145.4 (41+). A true
// breath before the void.
// FROSTCHEST CLEAR-PATCH (x16, z-0.9 — the >=6u law held 6x over): nearest fixed threat of ANY kind is
// herald #1's beam floor at x53/y3.5+ (37u); nothing at all patrols west of it. Opening is a deliberate,
// safe act; the ambush spawns on the kit ring with its 1s grace.
//
// ---- THE STILL COURT WINDOW (pinned honest, the brief's law): herald #2 period 7.8 phase 1.2 → the beam
// threatens the seat during t ≡ 5.8..6.8 and 13.6..14.6 (mod 15.6). Wisp #2 (x0 139 → x1 154, one-way 7.8,
// LCM 15.6) crosses the seat x146 at t 3.64 east / 11.96 west → wake+head threaten ~t 3.0..5.0 and
// 11.3..13.3. Combined threat: [3.0..5.0] [5.8..6.8] [11.3..13.3] [13.6..14.6]. SAFE SIT WINDOWS:
// t ≡ 6.8..11.3 (4.5s) and 14.6..3.0 (4.0s) — the 3.0s sit fits BOTH with >=1.0s slack, twice per 15.6s.
// Findable (both sweeps are loud and visible), repeatable (fixed clocks, cull-pinned), forgiving enough to
// be brave rather than cruel. The bravest quiet in the game, honestly kept. ----
//
// ---- THE CALENDAR WALL, ENGINEERED (why this level ghosts the kit's central collider): ghost boxes do NOT
// carve holes in this engine (03_physics skips them — nothing subtracts). w10AdventWall's own y0..h wall
// box would therefore seal BOTH doorways shut forever, and its door plug guards only the front half-lane
// (z 0.2..1.6 — a z=0 runner squeezes past at z<0.2). So: the kit call keeps the whole VISUAL contract
// (wall face, sliding silver doors, gilt frames, curtsying numbers, the 0.7s glow) and this level owns the
// collision faithfully — the kit's central box is re-typed 'ghost' the line after the call (captured by
// index; it is the call's first addBox), solid segments are added between/above the doorways, and each
// doorway gets a FULL-DEPTH backer collider that mirrors its plug's open/shut type every frame in
// updateW10L5. Shut doors truly shut; open doors are truly the way through; a door closing on a straddler
// ejects along x (smallest-axis push — analyzed, harmless). ----

// ---- THE QUIET PROP: beside the great doors, a court coat rack — five gilt hooks under a carved rail,
// four hung with the court's folded velvet (ten thousand years of perfect manners, cloaks kept brushed),
// and ONE small hook set lower than the rest, child-height, empty, polished brighter than all the others
// from being dusted every single day. Under it, a tiny brass plate with five careful letter-strokes:
// GUEST. Never signposted; fully baked. Story-readers stop. Everyone else walks past. ----
function w10l5CoatRack(x, z){
  const g = new THREE.Group();
  const pole = mesh('cyl',[0.09,0.12,2.3,7], mat(0x4a3a28)); pole.position.set(x,1.15,z); g.add(pole);
  const foot = mesh('cyl',[0.34,0.4,0.1,8], mat(W10PAL.regalG)); foot.position.set(x,0.05,z); g.add(foot);
  const rail = mesh('box',[1.9,0.12,0.14], mat(0x4a3a28)); rail.position.set(x,2.2,z); g.add(rail);
  const crest = mesh('sph',[0.12,6,5], mat(W10PAL.regalG)); crest.position.set(x,2.36,z); g.add(crest);
  for(let i=0;i<4;i++){                                  // the court's cloaks — kept, brushed, waiting
    const hx = x-0.78+i*0.52;
    const hook = mesh('tor',[0.05,0.018,4,8,Math.PI], mat(W10PAL.regalG)); hook.position.set(hx,2.1,z+0.08); hook.rotation.z=Math.PI; g.add(hook);
    const cloak = mesh('cone',[0.19,rand(0.75,0.95),6], mat(i%2?W10PAL.velvet:W10PAL.velvetD)); cloak.position.set(hx,1.62,z+0.06); g.add(cloak);
    const clasp = mesh('sph',[0.035,5,4], mat(W10PAL.regalS)); clasp.position.set(hx,2.0,z+0.14); g.add(clasp);
  }
  // the small hook — lower, empty, polished brightest; the plate with five careful strokes: G U E S T
  const small = mesh('tor',[0.045,0.018,4,8,Math.PI], emat(W10PAL.regalG, W10PAL.regalG, 0.55)); small.position.set(x+0.55,0.95,z+0.08); small.rotation.z=Math.PI; g.add(small);
  const arm = mesh('box',[0.06,0.5,0.06], mat(0x4a3a28)); arm.position.set(x+0.55,1.3,z); g.add(arm);
  const plate = mesh('box',[0.34,0.12,0.02], mat(W10PAL.regalG)); plate.position.set(x+0.55,0.68,z+0.06); g.add(plate);
  for(let i=0;i<5;i++){ const stroke = mesh('box',[0.03,0.06,0.012], emat(0xf4e8c0,0xd8b84a,0.5)); stroke.position.set(x+0.44+i*0.055,0.68,z+0.075); g.add(stroke); }
  return g;
}

// ---- TAPESTRY CLIMB: a velvet runner hung from a gilt rail — the palace's climbable (the caller adds the
// climb volume; this is the look: rail, runner, fringe, one embroidered flame nobody has lit in an age) ----
function w10l5Tapestry(x, topY, len){
  const g = new THREE.Group();
  const rail = mesh('cyl',[0.06,0.06,1.5,6], mat(W10PAL.regalG)); rail.rotation.z=Math.PI/2; rail.position.set(x,topY,0); g.add(rail);
  for(const s of [-1,1]){ const knob = mesh('sph',[0.09,6,5], mat(W10PAL.regalG)); knob.position.set(x+s*0.78,topY,0); g.add(knob); }
  const run = mesh('box',[0.72,len,0.06], mat(W10PAL.velvet)); run.position.set(x,topY-0.1-len/2,-0.12); g.add(run);
  const hem = mesh('box',[0.78,0.1,0.08], mat(W10PAL.regalG)); hem.position.set(x,topY-0.12-len,-0.12); g.add(hem);
  for(let i=0;i<3;i++){ const tas = mesh('cone',[0.035,0.14,4], mat(W10PAL.regalG)); tas.rotation.x=Math.PI; tas.position.set(x-0.24+i*0.24,topY-0.22-len,-0.12); g.add(tas); }
  const flame = mesh('sph',[0.1,6,5], emat(0xd8b84a,0x8a6a1e,0.35)); flame.scale.set(0.7,1.25,0.4); flame.position.set(x,topY-0.55-len*0.45,-0.08); g.add(flame);
  return g;
}

function buildW10L5(G){
  const S = G.scene;
  levelBegin(G);
  G.camMinY = -1.2;                          // the fountain recess dips to -1.8; give the camera the court's floor

  const FLOOR = W10PAL.iceD;                 // palace pavers — grippy court stone-ice
  const deco = new THREE.Group();            // all static scenery bakes to one draw call at the tail

  // =============================== BEAT 1 — THE PROCESSION ROAD (x -8..44) ===============================
  groundX(G, -8, 44, FLOOR);
  G.ents.add(new Checkpoint(2, 0, 1.6, 0, {noLight:true}));   // CP0 — the only start you get (exam law)
  signPost(G, 6, 1.7, -0.1, "THE THRONE APPROACH. The court will now examine the guest. Fountains rise when the basin sings. Doors keep the calendar. Soldiers keep the step. The court has kept its manners for ten thousand years. Kindly keep yours.");
  // the processional — a velvet runner down the road's spine, gilt-edged, brushed this morning as every morning
  { const runr = mesh('box',[46,0.05,1.6], mat(W10PAL.velvetD)); runr.position.set(15,0.06,0); deco.add(runr);
    for(const s of [-1,1]){ const trim = mesh('box',[46,0.06,0.12], mat(W10PAL.regalG)); trim.position.set(15,0.07,s*0.86); deco.add(trim); } }
  candyLine(G, [[8,0.9,0],[12,0.9,0]], 3);
  // THE FROSTCHEST — the gamble, in its clear pocket (ledger in the header: nothing patrols within 37u)
  { const c = new FrostChest(16, 0, -0.9, 0.25); G.coffins.push(c); G.ents.add(c); }
  G.ents.add(new Crow(24, 0.95, 2.1));       // court crow #1 — flaps off when neared
  // cold candelabra pairs flanking the road (emissive only — the real-light budget is spent elsewhere)
  for(const cx of [10, 26, 38]){ for(const s of [-1,1]){
    const cnd = mesh('cyl',[0.07,0.1,1.1,6], mat(W10PAL.regalS)); cnd.position.set(cx,0.55,s*2.3); deco.add(cnd);
    const fl = mesh('sph',[0.08,5,4], emat(0x7ae8ff,0x7ae8ff,0.9)); fl.position.set(cx,1.2,s*2.3); deco.add(fl); } }
  signPost(G, 40, 1.7, 0.1, "The fountain court. The fountain bows before it rises; the herald bows before he blows. Ride the one between the other. The court finds haste... acceptable.");

  // =============================== BEAT 2 — THE FOUNTAIN COURT (x 44..76): geysers under herald fire ===============================
  // the sunken plaza: floor -1.8 (a safe recess, not a pit — dropping in is free, geyser A is the only way on)
  platform(G, 49, -1.8, 0, 10, 10, 0x8298bc);
  { const face = mesh('box',[0.4,1.9,10], mat(W10PAL.wall)); face.position.set(44.2,-0.85,0); deco.add(face); }   // west recess face
  // frozen-spray dressing: the fountain's last plume, kept mid-air by the cold
  for(let i=0;i<7;i++){ const shard = mesh('cone',[rand(0.06,0.12),rand(0.3,0.7),5], emat(W10PAL.ice,0x4a9ed0,0.4));
    shard.position.set(49+rand(-2.6,2.6), -1.5+rand(0,0.5), rand(-2,2)); shard.rotation.z=rand(-0.4,0.4); deco.add(shard); }
  // GEYSER A — the way out (period 5.6: tele 2.0..2.7, rise 2.7..3.2, HOLD 3.2..5.55, shatter — harmless)
  w10Geyser(G, 50.4, {period:5.6, phase:0, height:4.8, holdT:2.4, baseY:-1.8});    // cap tops at 3.0
  candyLine(G, [[46,-0.9,0],[49,0.3,0],[50.8,2.2,0]], 3);                          // the ride, traced
  // THE LEDGE PIER (x54..62.4, top 2.8) — recess east wall AND the herald's stage in one mass
  { const pier = mesh('box',[8.4,4.6,10], mat(W10PAL.wall)); pier.position.set(58.2,0.5,0); deco.add(pier);
    const lip = mesh('box',[8.4,0.12,10.2], mat(0xc2d2ec)); lip.position.set(58.2,2.86,0); deco.add(lip); }
  G.world.addBox(58.2, -1.8, 0, 8.4, 4.6, 10, {});
  // HERALD #1 — on the pier, horn west (period 5.6 phase 4.4 → beam window t≡0.4..1.4 while geyser A is
  // DOWN; every A-ride at 3.2..4.95 crosses a silent lane). Beam x53..57.2 at y3.5..4.2: ledge-standers hop
  // it (tap 1.8 over-clears the 1.4 needed); cap-riders near the top are why the phase math exists.
  // FAST LINE: one stomp silences the lane for the whole run. cull-pinned for the geyser promise.
  { const h1 = new FrostHerald(G, 58, 2.8, 0, {phase:4.4, period:5.6, beamY:3.85, reach:4.2, dir:-1}); h1.cull=false; G.ents.add(h1); }
  candyLine(G, [[55.6,4.4,0],[56.8,4.4,0]], 2);                                    // the beam-hop apex, traced
  // GEYSER B — pier top to gallery (period 5.6 phase 2.8 → HOLD t≡0.4..2.75, the beat after A's)
  w10Geyser(G, 60.8, {period:5.6, phase:2.8, height:2.6, holdT:2.4, baseY:2.8});   // cap tops at 5.4
  candyLine(G, [[61.9,6.3,0],[63.6,6.1,0]], 2);
  // THE GALLERY (x63..74, y5.2) — the high road; two support columns below (baked, background)
  platform(G, 68.5, 5.2, 0, 11, 3, W10PAL.wall);
  for(const gx of [64.5, 72.5]){ const colm = mesh('cyl',[0.34,0.42,4.7,8], mat(0x2a3a60)); colm.position.set(gx,2.35,-1.4); deco.add(colm); }
  candyLine(G, [[66,6.1,0],[69,6.1,0],[72,6.1,0]], 3);
  // ICICLE #1 — over the gallery from a court chandelier (walk-dodge under the herald's learned language)
  { const chain = mesh('cyl',[0.03,0.03,1.8,4], mat(0x2a3a60)); chain.position.set(67.5,10.3,0); deco.add(chain);
    const arm = mesh('box',[1.6,0.14,0.14], mat(W10PAL.regalG)); arm.position.set(67.5,9.4,0); deco.add(arm);
    for(const s of [-1,1]){ const cf = mesh('sph',[0.07,5,4], emat(0x7ae8ff,0x7ae8ff,0.9)); cf.position.set(67.5+s*0.7,9.3,0); deco.add(cf); } }
  G.ents.add(new SpikeIcicle(G, 67.5, 9.2, {period:4.4, phase:0.0, len:1.2, floorY:5.2}));
  // ground resumes under/past the gallery — the LOW ROAD (drop east off the pier or gallery, run beneath)
  groundX(G, 62.4, 113.4, FLOOR);
  candyLine(G, [[65,0.9,0],[70,0.9,0]], 2);
  platform(G, 75.6, 3.4, 0, 1.8, 2.4, FLOOR);                                      // the gallery's descent step
  G.ents.add(new Crow(74.4, 6.15, 2.2));     // court crow #2 — on the gallery rail, watching the last pipe

  // =============================== BEAT 2b — THE LAST PIPE (x 76..86): the GP chord (expert) ===============================
  // THE TALLEST GEYSER (period 6.4: rise 3.3..3.8, HOLD 3.8..6.35 — the longest, proudest column in the game)
  w10Geyser(G, 78, {period:6.4, phase:0, height:5.0, holdT:2.6, baseY:0});         // cap tops at 5.0
  // THE LAST PIPE of the great court organ — one pipe, still tuned, still proud (period 4.8, chord at 0.7;
  // vy16 ≈ +5.8 from its 2.2 mouth). Standing on the mouth at the chord = launched. Harmless, glorious.
  w10Organ(G, {x:81.4, pipes:1, period:4.8, phase:0, vy:16});   // audit fix: the kit signature is (G, opts) — 81.4 was being swallowed as opts and the pipe built at x=0
  candyLine(G, [[78.8,5.9,0],[80.2,4.6,0],[81.4,3.4,0]], 3);                       // cap → mouth, traced (the skill-gate telegraph law)
  // THE ROSE-WINDOW LEDGE — GP idx 2 (one real light rides in the pumpkin itself)
  platform(G, 84, 6.6, 0, 1.9, 2.2, W10PAL.regalS);
  G.ents.add(new GoldPumpkin(84, 7.5, 0, 2));
  candyLine(G, [[83.6,7.9,0],[84.4,7.9,0]], 2);
  { const cc = W10PAL.aur3;                                                        // the rose window itself, close enough to touch
    const rose = mesh('cyl',[1.5,1.5,0.2,12], emat(cc, cc, 0.55)); rose.rotation.x=Math.PI/2; rose.position.set(84.4,9.2,-2.0); deco.add(rose);
    const rim = mesh('tor',[1.5,0.14,5,16], mat(0x2a3a60)); rim.position.set(84.4,9.2,-2.0); deco.add(rim);
    for(let sp=0;sp<6;sp++){ const a=sp/6*TAU; const spoke = mesh('box',[0.09,1.4,0.07], mat(0x2a3a60)); spoke.position.set(84.4+Math.cos(a)*0.7, 9.2+Math.sin(a)*0.7, -1.85); spoke.rotation.z=a; deco.add(spoke); } }

  // =============================== BEAT 3 — THE CALENDAR WALL (x 86..102): doors through flurry lanes ===============================
  // FLURRY LANES — the last trio's survivors, rolling fixed 4.2s clocks LOCKED to the wall's 8.4 (two rolls
  // per calendar page; the whole beat is one composite machine). Lane 1 dies against the last pipe's case
  // (a shatter both can be proud of); lane 2 owns the deep pocket. cull-pinned to hold the lock.
  { const f1 = new FlurryTriplet(G, 90.6, 0, 0, {x1:83, speed:4.0, phase:0.0, pause:2.3}); f1.cull=false; G.ents.add(f1); }
  { const f2 = new FlurryTriplet(G, 93.6, 0, 0, {x1:86, speed:4.0, phase:2.1, pause:2.3}); f2.cull=false; G.ents.add(f2); }
  // ICICLE #2 — over the deep waiting pocket (the pocket's last 1.7u before the wall is flurry-free: wait
  // THERE — layered pressure with an honest refuge, never a cheap hit)
  { const arm2 = mesh('box',[1.5,0.14,0.14], mat(W10PAL.regalG)); arm2.position.set(92.6,7.5,0); deco.add(arm2);
    const ch2 = mesh('cyl',[0.03,0.03,2.4,4], mat(0x2a3a60)); ch2.position.set(92.6,8.8,0); deco.add(ch2); }
  G.ents.add(new SpikeIcicle(G, 92.6, 7.4, {period:4.2, phase:1.0, len:1.2, floorY:0}));
  // THE CHAIN — up to the high door's west stub (anchored on a crown bracket; a flurry-free perch)
  { const brk = mesh('box',[1.6,0.18,0.2], mat(W10PAL.regalG)); brk.position.set(96.5,6.45,0); deco.add(brk); }
  w5Chain(G, 95.9, 0.3, 6.2);
  candyLine(G, [[95.9,2.6,0],[95.9,4.4,0],[97.2,4.2,0]], 3);
  // THE ADVENT WALL — kit visuals + this level's collision (see the header's engineering note)
  const wallColAt = G.world.cols.length;
  const wallDoors = w10AdventWall(G, {x:98, w:1.7, h:10, doors:[{dx:0, dy:0, slot:0},{dx:0, dy:3.4, slot:2}], period:8.4, openT:2.8, phase:0});
  G.world.cols[wallColAt].type = 'ghost';                        // the kit's central box — visual only (ghost carves nothing in this engine)
  G.world.addBox(98, 2.3, 0, 1.7, 1.1, 3, {});                   // solid between the doorways
  G.world.addBox(98, 5.7, 0, 1.7, 4.3, 3, {});                   // solid above the high door to the crown (10: chain 6.2 + double 9.5 stays under)
  const backA = G.world.addBox(98, 0,   0, 1.7, 2.3, 3, {});     // full-depth door backers — mirror the plugs each frame
  const backB = G.world.addBox(98, 3.4, 0, 1.7, 2.3, 3, {});
  // doorway insets — dark arches behind the sliding panels so an open door READS open
  for(const dy of [0, 3.4]){ const inset = mesh('box',[1.6,2.2,0.05], emat(0x0a0c1c,0x141c34,0.25)); inset.position.set(98, dy+1.15, 1.51); deco.add(inset); }
  // the crown — crenellation + the calendar rosette
  { const crown = mesh('box',[3.4,0.5,3.2], mat(0x2a3a60)); crown.position.set(98,10.25,0); deco.add(crown);
    for(let i=0;i<4;i++){ const merlon = mesh('box',[0.5,0.5,3.2], mat(0x2a3a60)); merlon.position.set(96.7+i*0.9,10.75,0); deco.add(merlon); }
    const ros = mesh('tor',[0.55,0.09,5,14], mat(W10PAL.regalG)); ros.position.set(98,11.6,0.2); deco.add(ros);
    const rosC = mesh('sph',[0.16,6,5], emat(0xd8b84a,0xd8b84a,0.7)); rosC.position.set(98,11.6,0.25); deco.add(rosC); }
  // the sill through the high doorway + the east landing
  platform(G, 98, 3.4, 0, 3.2, 2.4, FLOOR);
  platform(G, 100.5, 3.4, 0, 1.8, 2.4, FLOOR);
  candyLine(G, [[96.6,0.9,0],[98,0.9,0],[99.4,0.9,0]], 3);                         // the floor-door thread
  signPost(G, 88.5, 1.7, -0.1, "The calendar wall keeps two appointments: the floor door keeps the early hour, the high door keeps the late. The numbers curtsy before closing. Snow is expected on the approach. Dress accordingly.");

  // =============================== BEAT 4 — THE LANTERN LANDING (x 102..113.4): the breath ===============================
  G.ents.add(new BonkLantern(G, 102.5, 1.5, 0, 'shield'));                         // armor before the void
  G.ents.add(new Checkpoint(104, 0, 1.6, 1));                                      // THE lantern — 54.9% (ledger above)
  candyLine(G, [[103,0.9,0],[105,0.9,0]], 2);
  signPost(G, 107.5, 1.7, 0.1, "The honor guard marches the abyss in perfect relay. Board on the beat. Change hats at the meeting of hats. Do not applaud; it startles no one.");
  G.ents.add(new Crow(110.5, 0.95, 2.2));    // court crow #3 — on the lip (a crow marks an edge, house law)

  // =============================== BEAT 5 — THE SOLDIER PARADE (x 113.4..134.2): the relay over the void ===============================
  // Four nutcrackers, one 4s beat (speed 2.4, span 4.8, P 2.0, phases 0/2/0/2): every transfer closes at a
  // shared endpoint — board S1 west-end at t≡0, S1→S2 at t≡2 (x~119.2), S2→S3 at t≡0 (x~123.6), S3→S4 at
  // t≡2 (x~128.0), hop off S4 at t≡0 (x132.6 → lip 134.2). A clean relay is 8.0s lip-to-lip, zero waiting.
  // Hats top at 1.2 (board rise 1.2, tap over-clears); the soldiers themselves NEVER bite (movers, not
  // enemies) — they wade the spike bed below, entirely unbothered, which is the joke and the mercy.
  pitDressing(G, 113.4, 134.2, 'winter', -2.75);
  w10Soldier(G, {x0:114.6, x1:119.4, y:-2.3, speed:2.4, phase:0});
  w10Soldier(G, {x0:119.0, x1:123.8, y:-2.3, speed:2.4, phase:2});
  w10Soldier(G, {x0:123.4, x1:128.2, y:-2.3, speed:2.4, phase:0});
  w10Soldier(G, {x0:127.8, x1:132.6, y:-2.3, speed:2.4, phase:2});
  candyLine(G, [[116.5,2.3,0],[121,2.3,0],[125.5,2.3,0],[130,2.3,0]], 4);          // the relay line, hat height
  // AURORA WISP #1 — ribbons BETWEEN the roads (y3.1 ±0.7): its fading wake nips hat-riders' ears on the
  // dips (head 2.45 vs wake floor 2.0) and brushes high-road boarding hops. Cross behind it on the fade.
  G.ents.add(new AuroraWisp(G, {x0:115, x1:133, y:3.1, period:6.5, amp:0.7, phase:0, color:0x58e0a8}));
  // THE HIGH ROAD — tapestry climb to the chandelier rail (heart + candy; the junction itch: its glow line
  // is visible from the boarding lip)
  deco.add(w10l5Tapestry(112.3, 5.0, 4.3));
  G.world.addBox(112.3, 0.2, 0, 0.9, 4.4, 1.2, {type:'climb'});
  candyLine(G, [[112.3,2.4,0],[112.3,3.9,0]], 2);
  platform(G, 115.2, 4.2, 0, 1.7, 2.2, W10PAL.regalS);
  platform(G, 119.9, 4.7, 0, 1.7, 2.2, W10PAL.regalS);
  platform(G, 124.6, 4.3, 0, 1.7, 2.2, W10PAL.regalS);
  platform(G, 129.4, 4.8, 0, 1.7, 2.2, W10PAL.regalS);
  for(const [px,py] of [[115.2,4.2],[119.9,4.7],[124.6,4.3],[129.4,4.8]]){          // chandelier rods (baked, background)
    const rod = mesh('cyl',[0.03,0.03,10.5-py,4], mat(0x2a3a60)); rod.position.set(px,(10.5+py)/2,-0.9); deco.add(rod);
    const cfl = mesh('sph',[0.07,5,4], emat(0x7ae8ff,0x7ae8ff,0.8)); cfl.position.set(px,py+0.9,-0.9); deco.add(cfl); }
  candyLine(G, [[117.8,5.6,0],[122.6,5.9,0],[127.4,5.7,0]], 3);
  G.ents.add(new Heart(124.6, 5.2, 0));                                            // the high road's mercy

  // =============================== BEAT 6 — THE STILL COURT (x 134.2..158.8): the guest chair ===============================
  groundX(G, 134.2, 158.8, FLOOR);
  { const runr2 = mesh('box',[22,0.05,1.6], mat(W10PAL.velvetD)); runr2.position.set(146.5,0.06,0); deco.add(runr2);
    for(const s of [-1,1]){ const trim2 = mesh('box',[22,0.06,0.12], mat(W10PAL.regalG)); trim2.position.set(146.5,0.07,s*0.86); deco.add(trim2); } }
  G.ents.add(new Heart(137.5, 0.9, 0));
  candyLine(G, [[137,0.9,0],[139,0.9,0]], 2);
  signPost(G, 140, 1.7, -0.1, "The antechamber is swept twice over: the herald keeps his hour, the ribbon keeps hers. Between the two, the hall holds one long quiet. The court trusts a guest to notice such things.");
  // THE DISTRICT WARP — the guest chair (purple lantern tell; sit 3 uninterrupted seconds; warp to x191 —
  // past the gauntlet, before the sign and the coat rack, so even the warp walks the last goodbye)
  G.ents.add(new ThroneShadow(G, 146, {warpX:191, candy:40}));
  // HERALD #2 — the hall's hour (period 7.8 phase 1.2, horn west over the seat; beam y0.65..1.35 — hop it in
  // passing, or stand behind him: he never turns). cull-pinned: his window with the ribbon is a PROMISE.
  { const h2 = new FrostHerald(G, 150.4, 0, 0, {phase:1.2, period:7.8, beamY:1.0, reach:4.2, dir:-1}); h2.cull=false; G.ents.add(h2); }
  // WISP #2 — the hall's ribbon (7.8 one-way, LCM 15.6 with the horn — the pinned windows in the header)
  G.ents.add(new AuroraWisp(G, {x0:139, x1:154, y:1.8, period:7.8, amp:0.55, phase:0, color:0xb08aff}));

  // =============================== BEAT 7 — THE LAST GAUNTLET (x 158.8..188): everything, composed ===============================
  pitDressing(G, 158.8, 162.3, 'winter');                                          // gap #1 — 3.5, tap law
  candyLine(G, [[159.6,1.6,0],[161.5,1.6,0]], 2);
  groundX(G, 162.3, 184.2, FLOOR);
  // KNIGHT #1 guards the landing (patrol 166..170; worst west lunge tip 161.8 — it contests the lip and
  // stops ON it, never floating over the void). MIRROR #1's hall is the landing itself: feint west, run east.
  G.ents.add(new FrostKnight(G, 168, 0, 0, {phase:0.4, range:2.0, dir:-1}));
  G.ents.add(new MirrorFrost(G, 165, 0, 0, {range:2.5}));
  // THE CAROL — the palace's only complete round, verses 0/1/2 west to east (one 6.9s clock; the singing IS
  // the telegraph; a stare gives a singer stage fright and silences the verse — the learned counter-verb)
  G.ents.add(new CarolBoo(G, 164, 2.6, 0, {verse:0, period:6.9, phase:0, range:9}));
  G.ents.add(new CarolBoo(G, 171.5, 2.9, 0, {verse:1, period:6.9, phase:0, range:9}));
  G.ents.add(new CarolBoo(G, 179, 2.6, 0, {verse:2, period:6.9, phase:0, range:9}));
  candyLine(G, [[163,0.9,0],[166,0.9,0],[169.5,0.9,0]], 3);
  G.ents.add(new FrostKnight(G, 174.6, 0, 0, {phase:1.7, range:2.2, dir:1}));
  candyLine(G, [[172.5,0.9,0],[175.5,0.9,0]], 2);
  // the final knot (the cap's worst pinch, held at 3): knight #3 + mirror #2 + verse 2, sharing 177.5..181.5
  G.ents.add(new FrostKnight(G, 179.5, 0, 0, {phase:3.1, range:2.0, dir:-1}));     // worst east lunge tip 184.9 (touch 185.68) — his boots overhang gap #2's lip (184.2) by 0.7 for under a second, ONLY chasing a player who watched his full 0.7s bow (K1's contest-the-lip law, mirrored east); patrol clamp recalls him — he never crosses the gap
  G.ents.add(new MirrorFrost(G, 181, 0, 0, {range:2.2}));                          // hall 178.8..183.2; touch tip 183.86 < lip 184.2
  candyLine(G, [[181,0.9,0],[183,0.9,0]], 2);
  pitDressing(G, 184.2, 188, 'winter');                                            // gap #2 — 3.8, tap law
  candyLine(G, [[184.9,1.6,0],[186.1,2.0,0],[187.3,1.6,0]], 3);

  // =============================== BEAT 8 — THE GREAT DOORS (x 188..196): exhale, prop, gate ===============================
  groundX(G, 188, 199, FLOOR);
  G.ents.add(new Crow(189.5, 0.95, 2.2));    // court crow #4 — it stays. It has seen every guest there was.
  candyLine(G, [[190,0.9,0],[192.5,0.9,0]], 2);
  signPost(G, 191.5, 1.7, -0.1, "The court is assembled. The seat is kept. It was always kept. Announce yourself.");
  // THE QUIET PROP — the coat rack, the one small empty hook, the plate that says GUEST (never signposted)
  deco.add(w10l5CoatRack(193.4, -1.35));
  // the great doors themselves, looming behind the gate arch
  for(const s of [-1,1]){
    const leaf = mesh('box',[2.3,8.5,0.35], mat(0x24304e)); leaf.position.set(196+s*1.25,4.25,-2.3); deco.add(leaf);
    const trim = mesh('box',[0.18,8.5,0.4], mat(W10PAL.regalG)); trim.position.set(196+s*0.25,4.25,-2.28); deco.add(trim);
    const ring = mesh('tor',[0.3,0.05,5,12], mat(W10PAL.regalG)); ring.position.set(196+s*1.5,3.6,-2.05); deco.add(ring);
  }
  { const lamp = new THREE.PointLight(0xffb85e, 24, 10); lamp.position.set(195,3.6,-1); S.add(lamp); }   // the doors' warm welcome — the palace's first
  exitGate(G, 196);

  // =============================== FOREGROUND · DECO TAIL · PARALLAX ===============================
  for(const [fx,fr] of [[35,0.3],[120,-0.35],[170,0.3]]){                          // foreground drape silhouettes (z>0 framing)
    const drape = mesh('cone',[0.9,2.4,5], mat(0x181228)); drape.position.set(fx,1.0,2.7); drape.rotation.z=fr*0.3; deco.add(drape);
  }
  { const bal = mesh('box',[2.4,0.2,0.3], mat(0x141c34)); bal.position.set(146,1.5,2.8); deco.add(bal);
    for(let i=0;i<3;i++){ const bp = mesh('cyl',[0.07,0.1,1.4,5], mat(0x141c34)); bp.position.set(145.2+i*0.8,0.7,2.8); deco.add(bp); } }
  S.add(bakeGroup(deco));

  // level-local glue for updateW10L5 (area-stamped against stale scenes): the door backers mirror the kit
  // plugs so shut doors are truly shut at full depth (the header's engineering note)
  G._l5 = { area:G.area, doors:[ {plug:wallDoors[0].plug, col:backA}, {plug:wallDoors[1].plug, col:backB} ] };

  w10Parallax(S, -8, 199);
  w10LevelFinish(G, -8, 199, null);          // null clutter: baked props must not float over the recess or the voids
  w10Clutter(G, -8, 43.5, 'palace');         // ...so the solid spans are cluttered manually (w7l4 precedent)
  w10Clutter(G, 62.6, 96.8, 'palace');
  w10Clutter(G, 99.6, 113, 'palace');
  w10Clutter(G, 134.6, 158.4, 'palace');
  w10Clutter(G, 162.7, 183.8, 'palace');
  w10Clutter(G, 188.4, 198, 'palace');

  return {spawnX: 0, exitX: 196};
}

function updateW10L5(G, dt){
  updateLevelCommon(G, dt);
  const L5 = G._l5; if(!L5 || L5.area !== G.area) return;
  // the calendar wall's full-depth truth: each doorway backer wears its plug's current type (open=ghost,
  // shut=solid) — one frame behind the kit's own clock at worst, deterministic always
  for(const d of L5.doors) d.col.type = d.plug.type;
}

W10_LEVELS.push({id:'w10l5', district:'w10', name:'THE THRONE APPROACH', build:buildW10L5, update:updateW10L5, parTime:195});
